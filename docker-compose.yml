services:
  keytrak-cleanup:
    build: .
    container_name: keytrak-cleanup
    ports:
      - "8088:8088"
    env_file:
      - .env
    volumes:
      - ./tmp:/app/tmp
      - ./logs:/app/logs
    restart: unless-stopped
